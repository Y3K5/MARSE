# Keeps the SHA-pinned GitHub Actions current. Pre-commit hooks are updated with
# `pre-commit autoupdate --freeze` (see CONTRIBUTING.md).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      github-actions:
        patterns: ["*"]
